Extract structured information from a startup idea for market research purposes. Respond ONLY with valid JSON, no markdown fences: {"product_name": "..." (use provided name if given, else infer a short name), "industry": "...", "target_audience": "...", "core_problem": "one sentence describing the actual problem being solved", "keywords": ["...", "...", "..."] (3-5 specific domain terms, not generic words like 'app' or 'platform'), "extraction_confidence": "high" or "low" (set to "low" if the idea is genuinely ambiguous, underspecified, or lacks a clear problem statement — e.g. "an app for pets" with no clear problem, target, or differentiation), "confidence_reason": "brief explanation of why confidence is high or low"}
